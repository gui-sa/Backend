using System;
using System.Globalization;

namespace UML{
    internal class UMLClass{
        static void Main(string[] args){
            float teste = 10f;
            double teste2 = teste;
            System.Console.WriteLine(teste2);
            
            
            //System.Console.Write("Entre os dados do Produto:\nNome: ");
            //string nome = Console.ReadLine();
            //decimal preco = ReceivingData.ReceivingDecimal("Preço: ");
            //int quantity = Convert.ToInt32(ReceivingData.ReceivingDecimal("Quantidade no estoque: "));
            //System.Console.WriteLine($"Dados do produto {nome}, $ {preco.ToString("f2")}, {quantity} unidades,Total: $ {(decimal)quantity*preco}\n");
            //int quantityAdd = Convert.ToInt32(ReceivingData.ReceivingDecimal("Digite o numero de produtos a ser adicionado no estoque: "));
            //quantity += Math.Abs(quantityAdd);
            //System.Console.WriteLine($"Dados do produto {nome}, $ {preco.ToString("f2")}, {quantity} unidades,Total: $ {(decimal)quantity*preco}\n");
            //int quantityRemove = Convert.ToInt32(ReceivingData.ReceivingDecimal("Digite o numero de produtos a ser removido do estoque: "));
            //quantity += -Math.Abs(quantityRemove);
            //System.Console.WriteLine($"Dados do produto {nome}, $ {preco.ToString("f2")}, {quantity} unidades,Total: $ {(decimal)quantity*preco}\n");
        }
    }
}